apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "storyblog.fullname" . }}-test-connection"
  labels:
    {{- include "storyblog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "storyblog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
